IF(UNIX)
ADD_CUSTOM_TARGET(check
  COMMAND sh run_tests.sh $<TARGET_FILE:svg2svgt-cli>
  WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
)
ELSEIF(WIN32)
ADD_CUSTOM_TARGET(check
  COMMAND run_test.bat $<TARGET_FILE:svg2svgt-cli>
  WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
)
ENDIF(UNIX)

