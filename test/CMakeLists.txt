IF(UNIX)
ADD_CUSTOM_TARGET(check
  COMMAND sh run_tests.sh $<TARGET_FILE:svg2svgt-cli>
  WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
)
ELSEIF(WIN32)
ADD_CUSTOM_TARGET(check
  COMMAND ${CMAKE_COMMAND} -E env "PATH=$<TARGET_FILE_DIR:svg2svgt>;$ENV{PATH}" run_tests.bat $<TARGET_FILE:svg2svgt-cli>
  WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
)
ENDIF(UNIX)
